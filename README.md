# CryptoRo - Инструкция по развертыванию проекта

## 🚀 Быстрый старт

После установки зависимостей и настройки базы данных:

```bash
php artisan migrate --seed
npm run build
php artisan serve
```

**Тестовый пользователь для входа в админ-панель:**
- **Email**: `admin@admin.com`
- **Пароль**: `password`

> 📍 URL админ-панели: `http://localhost:8000/admin/login`

---

## 📋 Содержание

1. [Требования к окружению](#требования-к-окружению)
2. [Установка зависимостей](#установка-зависимостей)
3. [Настройка окружения](#настройка-окружения)
4. [Настройка базы данных](#настройка-базы-данных)
5. [Сборка фронтенда](#сборка-фронтенда)
6. [Запуск проекта](#запуск-проекта)
7. [Доступ к админ-панели](#доступ-к-админ-панели)
8. [Структура проекта](#структура-проекта)

---

## 🔧 Требования к окружению

### Обязательные компоненты:

1. **PHP 8.2 или выше**
   - Проверка версии: `php -v`
   - Расширения PHP:
     - `pdo_sqlite` (для работы с SQLite)
     - `mbstring`
     - `openssl`
     - `json`
     - `tokenizer`
     - `xml`
     - `ctype`
     - `fileinfo`

2. **Composer** (менеджер зависимостей PHP)
   - Скачать: https://getcomposer.org/
   - Проверка: `composer --version`

3. **Node.js 18.x или выше** и **npm**
   - Скачать: https://nodejs.org/
   - Проверка: `node -v` и `npm -v`

4. **SQLite** (встроен в PHP, дополнительная установка не требуется)

### Рекомендуемые инструменты:

- **Git** для работы с версиями
- **OpenServer / XAMPP / Laragon** (для Windows) или встроенный веб-сервер PHP

---

## 📦 Установка зависимостей

### Шаг 1: Клонирование проекта (если используется Git)

```bash
git clone <repository-url>
cd test.loc
```

### Шаг 2: Установка PHP зависимостей

```bash
composer install
```

Эта команда установит все необходимые пакеты Laravel и их зависимости.

### Шаг 3: Установка Node.js зависимостей

```bash
npm install
```

Эта команда установит все необходимые пакеты для фронтенда (Vue.js, Vite, Tailwind CSS и др.).

---

## ⚙️ Настройка окружения

### Шаг 1: Создание файла .env

Если файл `.env` отсутствует, создайте его на основе `.env.example`:

**Windows (PowerShell):**
```powershell
Copy-Item .env.example .env
```

**Linux/Mac:**
```bash
cp .env.example .env
```

Или вручную создайте файл `.env` в корне проекта.

### Шаг 2: Генерация ключа приложения

```bash
php artisan key:generate
```

Эта команда сгенерирует уникальный ключ шифрования для вашего приложения.

### Шаг 3: Настройка переменных окружения

Откройте файл `.env` и настройте следующие параметры:

```env
APP_NAME="CryptoRo"
APP_ENV=local
APP_KEY=base64:... (генерируется автоматически)
APP_DEBUG=true
APP_URL=http://localhost:8000

# База данных (SQLite по умолчанию)
DB_CONNECTION=sqlite
DB_DATABASE=database/database.sqlite

# Если используете MySQL вместо SQLite:
# DB_CONNECTION=mysql
# DB_HOST=127.0.0.1
# DB_PORT=3306
# DB_DATABASE=your_database_name
# DB_USERNAME=your_username
# DB_PASSWORD=your_password
```

---

## 🗄️ Настройка базы данных

### Вариант 1: SQLite (по умолчанию, рекомендуется для разработки)

1. Убедитесь, что файл базы данных существует:

**Windows (PowerShell):**
```powershell
New-Item -ItemType File -Path "database\database.sqlite" -Force
```

**Linux/Mac:**
```bash
touch database/database.sqlite
```

2. Убедитесь, что в `.env` указано:
```env
DB_CONNECTION=sqlite
DB_DATABASE=database/database.sqlite
```

### Вариант 2: MySQL/MariaDB

1. Создайте базу данных в MySQL:
```sql
CREATE DATABASE your_database_name CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
```

2. Обновите `.env`:
```env
DB_CONNECTION=mysql
DB_HOST=127.0.0.1
DB_PORT=3306
DB_DATABASE=your_database_name
DB_USERNAME=your_username
DB_PASSWORD=your_password
```

### Выполнение миграций

После настройки базы данных выполните миграции:

```bash
php artisan migrate
```

Эта команда создаст все необходимые таблицы в базе данных.

### Заполнение базы данных начальными данными (Seeder)

Выполните команду для заполнения базы данных тестовыми данными:

```bash
php artisan db:seed
```

**Что делает сидер (`DatabaseSeeder`):**

1. **Создает тестового администратора:**
   - **Email**: `admin@admin.com`
   - **Пароль**: `password`
   - **Имя**: `Admin`

2. **Заполняет начальные данные:**
   - Пункты меню (через `MenuItemSeeder`)
   - Преимущества (через `AdvantageSeeder`)

> 💡 **Примечание**: Сидер находится в файле `database/seeders/DatabaseSeeder.php`. Вы можете изменить тестовые данные, отредактировав этот файл.

**Альтернативный способ (миграции + сидер одной командой):**

```bash
php artisan migrate --seed
```

Эта команда выполнит миграции и сразу запустит сидер.

---

## 🎨 Сборка фронтенда

### Режим разработки (с hot-reload)

```bash
npm run dev
```

Эта команда запустит Vite dev-сервер, который будет автоматически пересобирать фронтенд при изменениях.

### Продакшн сборка

```bash
npm run build
```

Эта команда создаст оптимизированную сборку для продакшна в папке `public/build`.

---

## 🚀 Запуск проекта

### Вариант 1: Встроенный сервер PHP (рекомендуется для разработки)

В одном терминале запустите сервер Laravel:

```bash
php artisan serve
```

Сервер будет доступен по адресу: `http://localhost:8000`

В другом терминале запустите Vite (для разработки):

```bash
npm run dev
```

### Вариант 2: Использование OpenServer/XAMPP/Laragon

1. Настройте виртуальный хост, указывающий на папку `public` проекта
2. Убедитесь, что в `.env` указан правильный `APP_URL`
3. Запустите Vite dev-сервер:
```bash
npm run dev
```

### Вариант 3: Полный dev-режим (все в одном)

Laravel предоставляет удобный скрипт для запуска всего сразу:

```bash
composer run dev
```

Эта команда запустит:
- PHP сервер
- Vite dev-сервер
- Queue worker
- Логи в реальном времени

---

## 🔐 Доступ к админ-панели

После выполнения миграций и сидеров:

1. Откройте в браузере: `http://localhost:8000/admin/login`

2. **Войдите с тестовыми учетными данными:**

   ```
   Email:    admin@admin.com
   Пароль:   password
   ```

   > ⚠️ **ВАЖНО**: Это тестовый пользователь, созданный сидером. После первого входа обязательно измените пароль администратора!

3. После успешного входа вы будете перенаправлены на `/admin` (дашборд админ-панели)

### Изменение пароля администратора

Через Laravel Tinker:

```bash
php artisan tinker
```

Затем выполните:
```php
$user = App\Models\User::where('email', 'admin@admin.com')->first();
$user->password = bcrypt('новый_пароль');
$user->save();
exit
```

---

## 📁 Структура проекта

```
test.loc/
├── app/                    # Основная логика приложения
│   ├── Http/
│   │   ├── Controllers/    # Контроллеры
│   │   └── Middleware/     # Middleware
│   ├── Models/             # Модели Eloquent
│   └── Providers/          # Service Providers
├── bootstrap/              # Файлы инициализации
├── config/                 # Конфигурационные файлы
├── database/
│   ├── migrations/         # Миграции базы данных
│   ├── seeders/           # Сидеры (начальные данные)
│   └── database.sqlite    # База данных SQLite
├── public/                 # Публичная директория (точка входа)
│   ├── assets/            # Статические ресурсы (изображения, SVG)
│   └── index.php          # Точка входа приложения
├── resources/
│   ├── css/               # Стили
│   ├── js/
│   │   ├── Components/    # Vue компоненты
│   │   ├── Pages/         # Inertia страницы
│   │   └── app.js         # Главный JS файл
│   └── views/             # Blade шаблоны
├── routes/
│   └── web.php            # Веб-маршруты
├── storage/               # Файлы хранилища (логи, кэш)
├── vendor/                # PHP зависимости (Composer)
├── node_modules/          # Node.js зависимости
├── .env                   # Переменные окружения (создать вручную)
├── composer.json          # PHP зависимости
├── package.json           # Node.js зависимости
└── vite.config.js         # Конфигурация Vite
```

---

## 🛠️ Полезные команды

### Очистка кэша

```bash
php artisan cache:clear
php artisan config:clear
php artisan route:clear
php artisan view:clear
```

### Сброс базы данных и повторное заполнение

```bash
php artisan migrate:fresh --seed
```

⚠️ **Внимание**: Эта команда удалит все данные из базы и заново создаст тестового пользователя (`admin@admin.com` / `password`).

### Запуск только сидера (без миграций)

Если нужно только заполнить базу данных тестовыми данными:

```bash
php artisan db:seed
```

Эта команда создаст тестового администратора и заполнит начальные данные (меню и преимущества).

### Запуск конкретного сидера

```bash
php artisan db:seed --class=DatabaseSeeder
php artisan db:seed --class=MenuItemSeeder
php artisan db:seed --class=AdvantageSeeder
```

### Создание нового контроллера

```bash
php artisan make:controller ControllerName
```

### Создание новой миграции

```bash
php artisan make:migration create_table_name
```

---

## 🐛 Решение проблем

### Ошибка: "SQLSTATE[HY000] [14] unable to open database file"

**Решение**: Убедитесь, что файл `database/database.sqlite` существует и у приложения есть права на запись в папку `database/`.

### Ошибка: "Vite manifest not found"

**Решение**: Выполните сборку фронтенда:
```bash
npm run build
```

Или запустите dev-сервер:
```bash
npm run dev
```

### Ошибка: "Class 'PDO' not found"

**Решение**: Установите расширение PHP `pdo_sqlite`:
- Windows: раскомментируйте `extension=pdo_sqlite` в `php.ini`
- Linux: `sudo apt-get install php-sqlite3`
- Mac: `brew install php-sqlite`

### Ошибка при установке npm пакетов

**Решение**: Очистите кэш и переустановите:
```bash
npm cache clean --force
rm -rf node_modules package-lock.json
npm install
```

---

## 📚 Дополнительная документация

- [Laravel Documentation](https://laravel.com/docs)
- [Vue.js Documentation](https://vuejs.org/)
- [Inertia.js Documentation](https://inertiajs.com/)
- [Vite Documentation](https://vitejs.dev/)
- [Tailwind CSS Documentation](https://tailwindcss.com/)

---

## 📝 Примечания

- Для продакшн-развертывания обязательно установите `APP_DEBUG=false` в `.env`
- Не коммитьте файл `.env` в репозиторий (он уже в `.gitignore`)
- Регулярно обновляйте зависимости: `composer update` и `npm update`
- Используйте `npm run build` перед деплоем на продакшн

---

## 👥 Поддержка

При возникновении проблем проверьте:
1. Версии PHP, Node.js и Composer соответствуют требованиям
2. Все зависимости установлены корректно
3. Файл `.env` настроен правильно
4. База данных создана и миграции выполнены
5. Права доступа к папкам `storage/` и `bootstrap/cache/` установлены корректно

---

**Удачной разработки! 🚀**
